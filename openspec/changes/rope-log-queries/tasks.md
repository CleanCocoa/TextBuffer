## 1. Contract Pin: Parity Zoo (green-first) and the Failing Perf Ratio (red)

- [x] 1.1 Extend `Tests/TextBufferTests/RopeBufferDriftTests.swift` with a `MARK: - Text Analysis Queries` block and a helper that asserts `lineRange(for:)` / `wordRange(for:)` on `RopeBuffer` **and** `SendableRopeBuffer` equal `MutableStringBuffer` for the same content and argument (follow the existing both-rope-buffers helper pattern, e.g. `assertContentInMatches`). These tests are **expected green on current `main`** — the O(n) implementations are correct; the zoo pins the contract the windowed implementations must not drift from.
- [x] 1.2 `lineRange` delimiter zoo, oracle-compared for a sweep of in-bounds ranges (zero-length at every delimiter-adjacent offset, ranges inside a line, ranges spanning multiple lines, ranges at document start and end, a range ending exactly on a line start): documents using `\n`, `\r`, `\r\n`, `U+0085` (NEL), `U+2028` (LS), `U+2029` (PS), mixed delimiters in one document, a zero-length range *between* `\r` and `\n` of a CRLF, a trailing line without a terminator, and a delimiter-free document (the whole document is one line).
- [x] 1.3 `lineRange` chunk-seam case: build a >4KiB ASCII document (so the rope has multiple leaves) whose CRLF pairs sit near leaf boundaries — e.g. lines sized to land `\r\n` around the 2048-byte chunk region — and sweep ranges whose line start/end scans must cross leaf seams and the 128-unit block boundaries (offsets 127/128/129 relative to a delimiter). Oracle-compared as in 1.1.
- [x] 1.4 `wordRange` zoo, oracle-compared: words with apostrophes (`don't`), hyphens (`well-known`), emoji words and emoji adjacent to letters, words crossing the initial 128-unit window radius (a word longer than 256 units), whitespace runs (a range inside a long run of spaces/newlines with the nearest word beyond the initial window radius — the D4 inconclusive-scan case), zero-length ranges at word starts/ends/middles, ranges at document start and end, and an all-whitespace document.
- [x] 1.5 Bounds behavior: both queries on both rope buffers throw `BufferAccessFailure.outOfRange` for a range past the end, a negative location, and `NSNotFound` — asserted identical to `MutableStringBuffer`'s throw for the same call (expected green; pins that the windowed rewrite keeps the guard-before-read shape).
- [x] 1.6 Create `Tests/TextBufferTests/RopeBufferQueryPerformanceTests.swift` with the **red-first** ratio tests, mirroring `testBulkInsertCostGrowsLinearlyWithInsertedLength` (`Tests/TextRopeTests/TextRopeInsertTests.swift:394-416`): build many-short-lines/short-words documents of 1 MiB and 4 MiB (e.g. 32-char lines of space-separated words), measure per-call `lineRange(for:)` at a mid-document zero-length range with `ContinuousClock`, best-of-3 after a warm-up, `XCTSkipIf` when the 1 MiB baseline is below the noise floor, and assert `large / small < 2.0` — size-independent queries predict ≈1×, the current O(n) materialization ≈4×. One test for `lineRange`, one for `wordRange`; cover `RopeBuffer` and `SendableRopeBuffer` (e.g. parameterize or assert the slower of the two).
- [x] 1.7 Run `swift test` and record the red list: 1.6's two ratio tests MUST fail (≈4× scaling); everything in 1.1-1.5 MUST pass. A parity failure here means the zoo found a pre-existing `MutableStringBuffer` divergence — stop, file it against DEF-012's history, and re-derive before implementing.
  > 2026-08-04: Red list confirmed against main before implementing. Parity zoo: 41/41 `RopeBufferDriftTests` green. Ratio tests red on all four variants — `lineRange` 3.99× (`RopeBuffer`) / 3.93× (`SendableRopeBuffer`), `wordRange` 2.19× / 2.19× — all above the 2.0 bound. Note on 1.6: implemented as four tests (one per query × buffer type) rather than two parameterized ones, same coverage; noise floor set to 1 ms (precedent uses 2 ms) so the 500-call windowed baseline (~4.5 ms measured post-fix) stays above it and the guard keeps asserting instead of skipping after the fix.

## 2. Green: Windowed `lineRange`

- [x] 2.1 Create `Sources/TextBuffer/TextRope+LineRange.swift` with an `internal` `TextRope` extension `lineRange(for: NSRange) -> NSRange`: backward block-walk from `location` for the nearest delimiter end, forward block-walk from the range end for the next delimiter end, reading `utf16CodeUnits(in:)` in 128-unit blocks scanned in memory (one descent per block — mirror `regionalIndicatorRunStart` in `Sources/TextBuffer/TextRope+ComposedSequences.swift`). Delimiter set with longest-match CRLF per design D2, including the cross-block `\r`-peek on both scan directions and the location-inside-CRLF rule.
- [x] 2.2 Wire `RopeBuffer.lineRange(for:)` (`Sources/TextBuffer/Buffer/RopeBuffer.swift:39-48`): keep the `contains(range:)` guard and `BufferAccessFailure.outOfRange` payload unchanged, replace the materializing body with the windowed extension call, and delete the `TODO: [M3 Rope Queries]` marker at `RopeBuffer.swift:46`.
  > 2026-08-04: Deviation — dropped `@inlinable` from `RopeBuffer.lineRange(for:)` (the new `wordRange` override is non-inlinable too): an `@inlinable` body cannot reference the `internal` windowed extensions (design D1 keeps them non-public), and promoting them to `@usableFromInline` would leak their private block-walk helpers. Guard shape and error payload unchanged.
- [x] 2.3 Add the explicit `SendableRopeBuffer.lineRange(for:)` override in `Sources/TextBuffer/Buffer/SendableRopeBuffer.swift` — same guard shape, windowed extension call — so it stops inheriting the O(n) default.
- [x] 2.4 Run the 1.2/1.3 parity zoo and the 1.6 `lineRange` ratio test: all green. If a parity case fails, fix the scan — never the test.

## 3. Green: Windowed `wordRange`

- [x] 3.1 Create `Sources/TextBuffer/TextRope+WordRange.swift` with an `internal` `TextRope` extension `wordRange(for: NSRange) -> NSRange`: materialize a scalar-aligned window (initial radius 128 UTF-16 units) via `content(in:)`, run the existing `computeWordRange(for:in:contentRange:)` (`Sources/TextBuffer/Buffer/Buffer+wordRange.swift`) window-locally, map the result back, and double the radius on any retry condition per design D4 — result touches a window edge with document beyond it, or the inconclusive whitespace-scan fallback while the window is not the whole document. No scalar-by-scalar reimplementation.
- [x] 3.2 Add the `RopeBuffer.wordRange(for:)` override (it currently inherits the default): `contains(range:)` guard, windowed extension call.
- [x] 3.3 Add the explicit `SendableRopeBuffer.wordRange(for:)` override — the second half of the critical inherited-defaults fix.
- [x] 3.4 Run the 1.4 zoo and the 1.6 `wordRange` ratio test: all green. Pay particular attention to the whitespace-run and window-radius-crossing cases — they exist to catch a missed retry condition.

## 4. Docs Lift, Defect Flip, and Bookkeeping

- [x] 4.1 Remove the two remaining `TODO: [M3 Rope Queries]` markers from the `TextAnalysisCapable` defaults (`Sources/TextBuffer/Buffer/TextAnalysisCapable.swift:46` and `:54`) — no rope buffer inherits those defaults anymore; the defaults' implementation itself is unchanged. Verify `grep -rn "M3 Rope Queries" Sources/` returns nothing.
- [x] 4.2 Update the `RopeBuffer` DocC (`RopeBuffer.swift:4-9`): drop the O(n)-queries exception, state that `lineRange(for:)` and `wordRange(for:)` are O(log n + result length) via windowed rope reads, with the degenerate note that a delimiter-free document makes the result the whole document. Update the `SendableRopeBuffer` DocC (`SendableRopeBuffer.swift:12-14`) identically.
- [x] 4.3 Check `Sources/TextBuffer/Documentation.docc/` (and the TextRope DocC catalog) for restatements of the O(n)-queries caveat added by `docs-rope-disclosure`; update any found to the O(log n + result length) statement.
  > 2026-08-04: No restatements found — grepped both catalogs for materialization/O(n)/text-analysis/large-document phrasing; the only O(n) mentions concern `NSMutableString`/`MutableStringBuffer` mutations, which are unrelated and stay. No edits needed.
- [x] 4.4 Update `DEFECTS.md`: flip DEF-012 to `fixed` (both halves — docs half by `docs-rope-disclosure` in 0.10.0, O(log n) queries by this change), strike the "O(log n) queries deferred to M3" disposition notes in the header, triage table, and defect body, naming this change ID.
- [x] 4.5 Update `CHANGELOG.md` under `## [Unreleased]` (create the section above `## 0.10.0` if absent, per keepachangelog convention): a `### Fixed`/`### Changed` entry stating that `lineRange(for:)`/`wordRange(for:)` on `RopeBuffer` and `SendableRopeBuffer` no longer materialize the full document — per-call cost is now O(log n + result length), results unchanged.
- [x] 4.6 Run `openspec validate rope-log-queries --strict` and the full `swift test` suite; both green before archiving.
