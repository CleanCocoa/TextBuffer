## 1. Red: DEF-001 Regression Tests

- [ ] 1.1 Create `Tests/TextRopeTests/NodeSplitPointTests.swift` with the manifestation 1 repro: `TextRope(String(repeating: "a", count: 2047) + "\r" + String(repeating: "b", count: 2047))`, then `insert("\n", at: 2048)`. Assert every leaf is ≤ `Node.maxChunkUTF8` and ≥ `Node.minChunkUTF8`, that `content` is unchanged apart from the inserted `\n`, and that no leaf ends in a bare `\r`. MUST fail today with leaves `[2047, 2049]`.
- [ ] 1.2 Add the manifestation 2 repro to the same file: build `"a"×2047 + "\r"`, `"m"×2048`, `"\n" + "b"×2047` (construction yields leaves `[2048, 2048, 2048]` — assert that precondition explicitly so the repro cannot silently stop reproducing), `delete(in: NSRange(location: 2048, length: 2048))`, then assert the same leaf-size and CRLF properties. MUST fail today with `[2047, 2049]`.
- [ ] 1.3 Add the manifestation 3 repro: `"a"×2048 + "b"×2048 + ("c"×1022 + "\u{1F600}" + "d"×1022)`, `delete(in: NSRange(location: 1000, length: 4095))`. Assert every leaf ≤ max; assert the resulting shape is `[1023, 1026]` **and** assert why it is the ADR-012 minimal-deviation outcome — the combined 2049-byte slice has no `Character` boundary in `[1024, 1025]` (boundary starvation), and the alternative boundary 1027 leaves a 1022-byte tail (larger shortfall). Then re-run the same delete-triggering merge on the result and assert the shape is a fixed point — the merge does not retry the failed redistribution.
- [ ] 1.4 Add the manifestation 4 repro at the `Node` level: a 2048-byte chunk with a 4-byte scalar straddling the midpoint at bytes `[1022, 1026)` plus a 1-byte ASCII insert (2049 bytes total). Assert `leafSplitPoint`/`leadingChunkEnd` yields the boundary at 1026 (left 1026, right 1023 — shortfall 1), not today's 1022 (shortfall 2). MUST fail today.
- [ ] 1.5 Add window-property tests driving `rebalancedChunks` directly across the arithmetic from design.md: `count` = 2048 (single chunk out), 2049, 3072, 4095, 4096; each with an all-ASCII slice (window has a boundary → two chunks, both in `[min, max]`) and with a cluster placed to straddle the window (`\r\n` at 4096 → three balanced chunks, each in `[min, max]`; a 4-byte scalar at 2049 → the minimal-deviation two-way split). Assert chunk count, per-chunk bounds, that no chunk seam falls inside a grapheme cluster, and that concatenation round-trips the input exactly.
- [ ] 1.6 Add the small-combination case for `repairCRLFSeam`: a combined slice well under `maxChunkUTF8` containing a `\r\n` seam MUST come back as one chunk, not two ~50/50 chunks. MUST fail today (`low > high` drops straight to the unbounded fallback).
- [ ] 1.7 Add a construction-path test: a string whose greedy chunk boundary at `maxChunkUTF8` lands inside a multi-byte scalar, asserting `TextRope(_:)` produces no leaf below `minChunkUTF8`. This is the latent twin of manifestation 4 in `TextRope+Construction.swift:25-41`.
- [ ] 1.8 Add the whole-cluster oversized-leaf case (ADR-012): a single grapheme cluster larger than `maxChunkUTF8` (e.g. a base character with enough combining marks to exceed 2048 UTF-8 bytes), exercised via construction and via insert-driven overflow. Assert the cluster ends up whole inside exactly one leaf — no chunk seam inside the cluster — the leaf's oversize is accepted, and `content` round-trips. Record today's observed behavior in the red run.
- [ ] 1.9 Run `swift test` and record the red list. 1.1, 1.2, 1.4, 1.6 MUST fail; 1.3 fails on the fixed-point/minimal-deviation assertions only; 1.5 and 1.7 fail on their straddling variants; 1.8 is expected red — if any test passes unexpectedly, the repro is wrong: re-derive before implementing.

## 2. Green: Window-Aware Split Point Core

- [ ] 2.1 In `Sources/TextRope/Node+Split.swift`, add `static func boundary(in:nearest:within:) -> String.Index?` — bidirectional `Character`-boundary search over a closed UTF-8 offset range, nearest to a target, ties to the lower offset (preserving today's backward-first order). This is the clamp DEF-001 is missing. `Character` boundaries only — the helper never yields a bare Unicode scalar boundary.
- [ ] 2.2 Add `static func leadingChunkEnd(in slice: Substring) -> String.Index` implementing design D2: `count <= maxChunkUTF8` → `endIndex`; `count >= maxChunkUTF8 + minChunkUTF8` → largest `Character` boundary ≤ `maxChunkUTF8`; otherwise the balanced window `[minChunkUTF8, count - minChunkUTF8]` search. Verify 1.4 and 1.7 pass.
- [ ] 2.3 Add the starved-band fallback used by 2.2's third branch: over the hard window `[count - maxChunkUTF8, min(maxChunkUTF8, count - 1)]`, pick the `Character` boundary minimizing `shortfall(p) = max(0, min - p) + max(0, min - (count - p))`, ties to the lower offset — the ADR-012 minimal-deviation split (design D3).
- [ ] 2.4 Implement the whole-cluster rule (ADR-012): when the hard window holds no `Character` boundary at all — a single grapheme cluster larger than `maxChunkUTF8` — do NOT split inside the cluster. Emit the cluster whole as one oversized chunk of whatever size it needs. There is no scalar-boundary escape hatch: no split point ever crosses a `Character` boundary. Verify 1.8 passes.
- [ ] 2.5 Replace `balancedSplitPoint` with `static func rebalancedChunks(in slice: Substring) -> [Substring]` implementing design D1: single chunk when `count <= maxChunkUTF8`; balanced two-way when the `[low, high]` window holds a boundary; balanced three-way redistribution when it does not and `count >= 3 * minChunkUTF8` (targets at `count / 3` and `2 * count / 3`, nearest `Character` boundary each, minimal deviation, ties to the lower offset — decided 2026-08-01: balanced, not the greedy left-heavy chunker); otherwise the 2.3 minimal-deviation split. Delete `splitPoint(in:targetUTF8:)` once no caller remains, or keep it private to 2.2 — do not leave two split-point entry points.
- [ ] 2.6 Rewrite `leafSplitPoint` as a thin wrapper over `leadingChunkEnd` (or delete it and call `leadingChunkEnd` from `splitLeaf` directly) and fold `TextRope+Construction.swift`'s `chunkEnd` onto the same helper, so construction, insert-overflow and merge all share one window rule.
- [ ] 2.7 Replace the doc comments at `Node+Split.swift:71` and `:87` (DEF-014): state the real trigger (a grapheme cluster spanning the whole window — reached deterministically by `\r\n` at `count == 4096`, not only by degenerate ZWJ chains) and the real residue (an oversized chunk, since `String` index rounding relocates a misaligned index rather than splitting a cluster). Document the ADR-012 regime at the top of the file: splits at `Character` boundaries only; bounds yield only under provable starvation.

## 3. Green: Call-Site Plumbing for 1–3 Chunks

- [ ] 3.1 Update `combinedLeaf` (`Sources/TextRope/TextRope+Delete.swift:149-159`) to consume `rebalancedChunks`: append all but the last chunk into `merged`, return the last. Keep the `combined.utf8.count <= maxChunkUTF8` fast path. Verify 1.2 and 1.3 pass.
- [ ] 3.2 Change `repairCRLFSeam` (`Sources/TextRope/TextRope+Insert.swift:70-88`) to return `[Node]` (decided 2026-08-01: sibling return, no `buildTree` spine rebuild): chunk 0 into `leftLeaf`, the last chunk into `rightLeaf`, middle chunk(s) returned as overflow siblings; when `rebalancedChunks` returns one chunk, put it all in `leftLeaf` and signal that `rightLeaf` must be removed (design D5).
- [ ] 3.3 Wire the return value into `insertIntoNode` (`TextRope+Insert.swift:47-53`): splice overflow siblings after `children[i]`, recompute summaries on both spines, and re-check `children.count > Node.maxChildren → splitInner()` so a seam repair can cascade like any other split. Verify 1.1 and 1.6 pass.
- [ ] 3.4 Verify merge no-retry (ADR-012): the delete-path merge scan accepts a leaf that is out of bounds under proven starvation without re-attempting redistribution — apply a follow-up no-op-adjacent operation to manifestation 3's `[1023, 1026]` shape and assert the shape is stable across operations (no oscillation, no repeated split attempt). 1.3's fixed-point assertion MUST be green.
- [ ] 3.5 Run `swift test`. All of section 1 MUST be green. Diff any other test that changed behavior and justify each one in this task's notes before touching an expectation — content and count assertions must be untouched by construction.

## 4. Invariant Validation (DEF-007)

- [ ] 4.1 Write the failing validator test first: extend `Tests/TextRopeTests/TreeInvariantValidation.swift` usage with cases asserting that (a) a hand-built tree containing an *unjustified* undersized leaf (one whose neighbour could absorb it) is rejected, (b) manifestation 3's starved `[1023, 1026]` is accepted, (c) an oversized leaf whose chunk is a single grapheme cluster is accepted, and (d) an oversized leaf containing more than one `Character` is rejected. Today's validator accepts (a) and rejects (b) — both wrong.
- [ ] 4.2 Update `verifyChunkSizes` (`TreeInvariantValidation.swift:52-61`) to the exact ADR-012 predicates: every leaf ≤ `maxChunkUTF8` unless its chunk is a single grapheme cluster (whole-cluster leaf); every leaf ≥ `minChunkUTF8` unless, for each adjacent sibling, the combined size exceeds `maxChunkUTF8` **and** the combined slice has no `Character` boundary in its `[low, high]` window. No fuzzy byte tolerances anywhere. Verify 4.1 passes.
- [ ] 4.3 Add a per-operation stress run to `Tests/TextRopeTests/TextRopeStressTests.swift`: a dedicated seed running ~2,000 mixed operations with `verifyTreeInvariants` after **every** operation (not `i % 100`), reporting seed and operation index on failure. This is the DEF-007 decision: one dedicated per-operation-validated seed alongside the four sampled 10,000-operation seeds.
- [ ] 4.4 Confirm the per-operation run reproduces DEF-001 when 2.5 is reverted locally (a one-line stash), then restore. If it does not, the seed does not exercise the seam and must be replaced by one that does — record which operation index trips it.
- [ ] 4.5 Keep the 1-in-100 sampling at `TextRopeStressTests.swift:532,681` and `TextRopeInsertTests.swift:238` (decided 2026-08-01), and add a comment at each site stating that per-operation coverage is provided by the dedicated 4.3 seed.
- [ ] 4.6 Record the wall-clock delta of `swift test` before and after 4.3 in the task notes.

## 5. Test Hygiene (DEF-014)

- [ ] 5.1 `Tests/TextRopeTests/NodeTests.swift:42-48` — rewrite `testLeafSplitPointAdjustsWhenMidpointLandsInsideMultiByteCharacter`: rename it to state the ADR-012 behavior (boundary starvation in the legal window; minimal-shortfall split), keep 1023 as the expected offset **only** alongside an assertion that 1027 is the sole alternative and has a larger shortfall, so the number is pinned by a reason instead of by history. Decided 2026-08-01: 1023 is correct by the minimal-deviation rule — the test is reframed as reasoned behavior, not a bug pin.
- [ ] 5.2 `Tests/TextRopeTests/TextRopeDeleteTests.swift:119-133` — `testDeleteCausingLeafMerge` must discriminate merged from un-merged: replace `leafChunkSizes(rope).count == 2` / `sum == 2100` with `XCTAssertEqual(leafChunkSizes(rope), [1050, 1050])` (window `[1024, 1076]`, target 1050, all-ASCII → exact). The current assertions also hold for the un-merged `[900, 1200]`.
- [ ] 5.3 `TextRopeDeleteTests.swift:306` — `testDeleteLeafMergeDoesNotSplitCRLF` executes no merge (leaves `[2047, 1602]`, deletion leaves the second at 1502, above `minChunkUTF8`). Reshape it so a merge actually fires across the `\r\n` seam — the manifestation 2 shape from 1.2 is the natural reshape — or rename it to `testDeleteNearCRLFKeepsPairTogether` and add a separate merging case. Do not leave the name claiming a merge.
- [ ] 5.4 `TextRopeDeleteTests.swift:108` — `testDeleteSpanningLeaves` spans leaves but never merges. Rename to say so, or extend the deletion so an edge leaf drops below `minChunkUTF8` and assert the merged shape.
- [ ] 5.5 `Tests/TextBufferTests/RopeBufferDriftTests.swift:49` — move `// MARK: - Delete` down to the first actual delete test; the insert-with-selection tests at `:51-84` belong under the `MARK: - Insert` block at `:26`.
- [ ] 5.6 Confirm 2.7 landed the `Node+Split.swift:71` doc-comment correction (DEF-014's fifth bullet) — it is implemented there, listed here so the DEF-014 checklist is complete in one place.

## 6. Extract Node+Merge.swift (DEF-006c)

- [ ] 6.1 Move the merge machinery — `mergeUndersizedChildren`, `mergeUndersizedLeaves`, `combinedLeaf`, `mergeUndersizedInnerNodes`, `combinedInner`, and the CRLF-seam helpers they share as appropriate — from `Sources/TextRope/TextRope+Delete.swift` into the spec-named `Sources/TextRope/Node+Merge.swift`, symmetric with `Node+Split.swift`. Pure file movement: relax `private` to `internal`/`static` access only as required, no logic edits (decided 2026-08-01, resolving DEF-006c's silent task rewrite in `m2-rope-delete`).
- [ ] 6.2 Run `swift test` — the full suite MUST stay green with zero expectation changes, and the diff MUST show movement only.

## 7. Verification and Bookkeeping

- [ ] 7.1 Run the full suite (`swift test`) clean, including the four 10,000-operation stress seeds and the new per-operation seed. Record the leaf-size histogram printed by the stress run before and after, so the shape change is visible.
- [ ] 7.2 Re-verify each DEF-001 repro by hand against the built library (all four snippets from `DEFECTS.md:13-33`) and confirm the observed leaf sizes match what the regression tests assert.
- [ ] 7.3 Update `DEFECTS.md`: mark DEF-001 and DEF-007 `fixed`; mark DEF-006's `Node+Merge.swift` bullet done (the rest of DEF-006 rides `foundation-free-textrope` and `docs-rope-disclosure`); mark DEF-014 partially — its `RopeTransferIntegrationTests.swift:126-143` bullet is assigned to `fix-rope-cow-and-equality-coverage` per the 2026-08-01 decisions.
- [ ] 7.4 Add a `CHANGELOG.md` entry under `[Unreleased]` → `Fixed` describing the chunk-size repair in user-visible terms (rope leaves could exceed or fall below their size bounds after CRLF-adjacent edits; bounds now follow ADR-012's grapheme-first rule), noting no API or content behavior change.
