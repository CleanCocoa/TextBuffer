## 1. Benchmarks First: Baseline Measurement and the Regression Pin

> Red-first honesty (proposal, design D5): no automated assertion in this change is red on current `main`. The size-independence ratio test in 1.2 passes today because the current windowed cost is already per-call constant (fixed ±128-unit window; only the O(log n) descent scales) — it is the permanent regression pin, not the red test. The red signal for this change is the **comparative measurement** in 1.1: rope-backed `unsafeCharacter(at:)` ~5× slower than `MutableStringBuffer` on ASCII before the fast path, near-parity after. Per the DEF-011 precedent (`perf-rope-equality-and-bulk-insert` recorded its measured 4.01× insert ratio in its archived tasks), the gap is recorded as before/after numbers here and in the CHANGELOG, not hard-asserted.

- [ ] 1.1 Create `Tests/TextBufferTests/RopeReadPerformanceTests.swift` with the comparative measurement: for identical documents — ~1 MiB plain ASCII, an emoji-heavy document (surrogate pairs and ZWJ chains), and a regional-indicator-run document — time a fixed batch of `unsafeCharacter(at:)` calls at offsets spread across the document on `RopeBuffer` and on `MutableStringBuffer`, and `print` a per-document summary line (per-call nanoseconds for each implementation and the ratio), following the `TextRopeStressTests` summary-print style (`TextRopeStressTests.swift:726`). No assertions on the cross-implementation ratio — report only
- [ ] 1.2 Add the size-independence ratio test to the same file: measure the same fixed batch of `unsafeCharacter(at:)` calls (offsets spread proportionally) on a 1 MiB and a 4 MiB ASCII document via `RopeBuffer`; assert the large/small time ratio is below 2× (document-proportional work would predict ≈4×; constant cost ≈1×, log-descent ≈1.1×). Mirror `testBulkInsertCostGrowsLinearlyWithInsertedLength`'s tolerance discipline exactly (`Tests/TextRopeTests/TextRopeInsertTests.swift:397-416`): `ContinuousClock`, min-of-3 per size, one warm-up round, `try XCTSkipIf` when the small baseline is below a ~2 ms noise floor, failure message naming both cost models. Loosen toward 3× only if CI shows instability (design, open question 2)
- [ ] 1.3 Run 1.1 and 1.2 against current `main` and record the baseline here in this file (per-call times and rope-vs-`MutableStringBuffer` ratios per document, plus the 1 MiB/4 MiB ratio). Confirm the expectations: 1.2 passes already (documenting that it is a pin, not a red test) and 1.1 shows the ~5× ASCII gap that motivates the change

## 2. Correctness Net: Green Before the Fast Path Exists

- [ ] 2.1 Add the mixed-content every-offset sweep to `Tests/TextBufferTests/RopeBufferDriftTests.swift` (extend the `MARK: - Composed Character Sequence Reads` block, reusing `assertUnsafeCharacterMatches`): build one document interleaving printable-ASCII prose with 😀 (surrogate pair), `e\u{301}` (combining mark), 👨‍👩‍👧‍👦 (ZWJ chain), `\r\n` line breaks, a 🇩🇪 flag run, and a lone regional indicator — each non-ASCII feature bracketed by ASCII on both sides so every fast-path-eligible/ineligible boundary occurs — and assert `unsafeCharacter(at:)` on `RopeBuffer` and `SendableRopeBuffer` equals `MutableStringBuffer` at **every** UTF-16 offset of the document
- [ ] 2.2 Extend 2.1 (or add a sibling test) with the adjacency cases stated explicitly: single ASCII characters wedged between two non-ASCII features (safe current unit, unsafe neighbors on both sides), ASCII immediately before and after the CRLF pair, and a document that starts and ends with non-ASCII — so the triple check's conservatism (fall-through when any neighbor is unsafe) and the document-edge-is-safe rule are each exercised at pinned offsets
- [ ] 2.3 Run the drift suite on current `main` — 2.1 and 2.2 MUST pass against the existing windowed path before any implementation work, proving they pin today's behavior and are not tuned to the fast path

## 3. Implement the Fast Path

- [ ] 3.1 In `Sources/TextBuffer/TextRope+ComposedSequences.swift`, add the guard block at the top of `composedCharacterSequence(at:)`, after the existing bounds `precondition`: one block read `utf16CodeUnits(in: max(0, utf16Offset - 1) ..< min(utf16Count, utf16Offset + 2))`; if every returned unit is in `0x20...0x7E` (a missing neighbor at a document edge counts as safe), return `String(UnicodeScalar(...))` of the current unit (block index `utf16Offset == 0 ? 0 : 1`) — no window, no `NSString`. Otherwise fall through to the existing `expandingWindow` call unchanged. Comment the parity argument with a pointer to the spec requirement and design D1/D2 (why the neighbors are checked; why CR/LF and all controls are excluded)
- [ ] 3.2 Confirm `expandingWindow`, `scalarAlignedStart`, `regionalIndicatorRunStart`, `isTrailSurrogate`, and both range-form APIs (`composedCharacterSequences(in:)`, `content(in:)`) are byte-for-byte untouched — the fall-through path and the range forms keep exactly the `fix-composed-sequence-reads` behavior
- [ ] 3.3 Run the full drift suite: 2.1/2.2 plus all existing composed-read sweeps (`assertUnsafeCharacterMatches` RI sweeps, `testFlagRunExceedingBackwardWalkCapMatchesMutableStringBuffer`, ZWJ/combining far-offset pins) and `Tests/TextBufferTests/TextRopeComposedSequencesTests.swift` — all green, no expectation edits anywhere

## 4. Verification and Bookkeeping

- [ ] 4.1 Re-run `RopeReadPerformanceTests` and record the after numbers alongside 1.3's baseline here: the comparative ASCII ratio should drop from ~5× to near-parity with `MutableStringBuffer`; emoji and RI documents stay within their baseline range (they take the fall-through, paying only the 3-unit probe); the 1 MiB/4 MiB ratio stays under the 2× bound
- [ ] 4.2 Run the full suite (`swift test`) — including `TextRopeStressTests` and the `SendableRopeBuffer` suites — to confirm no observable behavior change anywhere
- [ ] 4.3 Update `DEFECTS.md`: mark DEF-011 `fixed` in full — read half closed by this change (name the fast path and the recorded before/after numbers), insert half previously closed by `perf-rope-equality-and-bulk-insert`; remove the read-half deferral note from the triage table's disposition or annotate it as resolved by `perf-read-fast-path`
- [ ] 4.4 Add a CHANGELOG.md entry under a new `## [Unreleased]` section (keepachangelog convention): `### Fixed` — rope-backed `unsafeCharacter(at:)` no longer pays window materialization and `NSString` bridging for printable-ASCII reads, restoring near-parity with `MutableStringBuffer` (cite the measured before/after per-call numbers from 4.1); note explicitly that results are unchanged at every offset and that non-ASCII reads keep the windowed path
