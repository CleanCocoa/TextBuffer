## 1. Red: DEF-016 Regression Tests

- [ ] 1.1 Create `Tests/TextRopeTests/GraphemeSeamRepairTests.swift` (`NodeSplitPointTests.swift` stays the DEF-001 split-point suite; adjacency repair is a distinct mechanism) with the insert repro: `TextRope(String(repeating: "a", count: 4096))`, then `insert("\u{301}", at: 2048)`. Assert the shape precondition first so the repro cannot silently stop reproducing (construction yields leaves `[2048, 2048]`), then assert `leafSeamViolations(in: rope.root)` is empty, `chunkSizeViolations(in: rope.root)` is empty, and `content` equals the oracle string with counts intact. MUST fail today with a seam violation after leaf 0 (leaves `[2048, 1025, 1025]`, cluster `a\u{301}` spanning the first seam).
- [ ] 1.2 Add the delete repro to the same file: `TextRope(String(repeating: "a", count: 2048) + "b\u{301}" + String(repeating: "c", count: 2045))` (assert the `[2048, 2048]` leaf precondition), then `delete(in: 2048..<2049)`. Assert `leafSeamViolations` empty, `chunkSizeViolations` empty, content/counts against the oracle. MUST fail today: the right leaf starts with `\u{301}` adjacent to leaf 0's trailing `a`, no merge fires.
- [ ] 1.3 Add end-to-end variants that pin repair legality, not shape: after each repro, run `verifyTreeInvariants(rope)` (full validator) and assert no chunk expectation beyond the ADR-012 predicates — a repair MAY legally emit a starvation-proven undersized leaf or a whole-cluster oversized leaf; assert via the predicate helpers, not byte-range pins.
- [ ] 1.4 Add predicate-boundary cases at the rope level: (a) ZWJ exposed at a seam by delete (e.g. delete the character before a `\u{200D}` that begins the next leaf) and (b) variation selector `\u{FE0F}` inserted at a leaf-boundary offset — both MUST end seam-violation-free. (c) Regional-indicator parity: a seam between a complete flag pair and a following lone RI is legal and MUST NOT be "repaired" into a different shape gratuitously (assert `leafSeamViolations` empty and content unchanged); a lone RI ending one leaf with a lone RI starting the next MUST be repaired. Build the RI shapes with hand-built `Node.inner`/`Node.leaf` trees where mutations cannot reach them, mirroring `TreeInvariantValidatorTests` style, and drive the producer through a mutation where they can.
- [ ] 1.5 Add a CRLF non-regression case in the same file asserting the generalized path still repairs the classic seam: the `fix-rope-split-point` manifestation-1 shape (`"a"×2047 + "\r"` then `insert("\n", at: 2048)`) stays green — CRLF is now the special case of the general rule.
- [ ] 1.6 Run `swift test --filter GraphemeSeamRepairTests` and record the red list. 1.1, 1.2, and 1.4's repair cases MUST fail today; 1.4(c)'s legality half and 1.5 SHOULD pass today. If a MUST-fail case passes, the repro is wrong: re-derive against DEFECTS.md before implementing.

## 2. Green: Generalize the Seam Predicate

- [ ] 2.1 In `Sources/TextRope/Node+Merge.swift`, replace `crlfSeam(between:and:)` with `graphemeSeam(between:and:)`: walk to the left subtree's rightmost leaf and the right subtree's leftmost leaf (the two walks the CRLF predicate already does), take `chunk.last` and `chunk.first` as `Character`s, and return whether their concatenation forms fewer than two `Character`s. Swift stdlib only — no Foundation (ADR-013); document at the declaration that the predicate must stay in agreement with the test-side `leafSeamViolations` validator, the same producer/validator pairing `Node.isBoundaryStarved` has for starvation, and that `\r\n` is the named special case (one `Character`).
- [ ] 2.2 Update the two merge-loop call sites in the same file — `mergeUndersizedLeaves` (the `crlfSeam` disjunct in its loop condition) and `mergeUndersizedInnerNodes` (likewise) — to `graphemeSeam`. The starvation fixed-point guard (accept without retrying) is untouched and applies to seam-driven combinations exactly as to size-driven ones.
- [ ] 2.3 Run `swift test --filter GraphemeSeamRepairTests`. 1.2 is NOT expected green yet (the delete gate still short-circuits, section 4); record which cases moved.

## 3. Green: Insert-Path Wiring

- [ ] 3.1 In `Sources/TextRope/TextRope+Insert.swift`, switch the post-splice seam check (the `i > 0 && Self.crlfSeam(...)` condition in `insertIntoNode`) to `graphemeSeam`. Per design D3 this one site is the complete adjacency set for insert and is already unconditional — never gated on undersized chunks; only the predicate changes.
- [ ] 3.2 Rename `repairCRLFSeam(between:and:)` to `repairGraphemeSeam(between:and:)` and update its doc comment: it repairs any grapheme seam by recombining the two seam leaves through `Node.rebalancedChunks` and splicing overflow, and its output MAY be an ADR-012 starved shape (minimal-shortfall undersized leaf, or whole-cluster oversized leaf) — the seam invariant is absolute, the byte bounds are not. No body changes expected; justify any that prove necessary.
- [ ] 3.3 Run `swift test --filter GraphemeSeamRepairTests` — 1.1 and 1.4(b) MUST now be green. Then run the full `swift test`; diff any other test that changed behavior and justify each touched expectation in this task's notes before amending it (content and count assertions must be untouched by construction).

## 4. Green: Delete-Path Wiring

- [ ] 4.1 In `Sources/TextRope/TextRope+Delete.swift`, rename `hasCRLFSeam(_:)` to `hasGraphemeSeam(_:)` over the generalized predicate. Keep it as an or-term of the merge gate in `deleteFromInner` so the seam scan runs unconditionally at every mutation-touched inner node — even when no child became undersized and none was removed (exactly repro 2's shape).
- [ ] 4.2 Run `swift test --filter GraphemeSeamRepairTests` — 1.2 and 1.4(a) MUST now be green, and the whole section-1 file green including the RI-parity and CRLF non-regression cases.
- [ ] 4.3 Run the full `swift test`. Diff and justify every changed expectation as in 3.3.

## 5. Stress Coverage: Grapheme Extenders in the Alphabet

- [ ] 5.1 Add `"\u{301}"` (combining acute), `"\u{200D}"` (ZWJ), and `"\u{FE0F}"` (variation selector) to `stressCharset` in `Tests/TextRopeTests/TextRopeStressTests.swift`, with a comment naming DEF-016: lone extenders at boundaries form seam-spanning clusters by adjacency, the class the alphabet never produced.
- [ ] 5.2 Harden the oracle comparison against canonical-equivalence masking (design D5): in the dedicated per-operation seed (`testPerOperationInvariantValidation`, seed `0xDEF007`), also assert `Array(rope.content.utf8) == Array(oracle.utf8)` so fidelity stays byte-exact now that combining marks make Swift `String ==` weaker than byte equality.
- [ ] 5.3 Run the per-operation-validated seed and the four sampled 10,000-operation seeds with the extended alphabet. All MUST be green (with sections 2–4 landed, seam violations are repaired at the causing operation). Record in the task notes whether the per-op seed actually produces extender-at-boundary adjacencies (e.g. temporarily revert 3.1 locally and confirm the seed goes red, then restore — mirroring fix-rope-split-point's 4.4 discipline; if the seed cannot reach the shape, record that finding rather than forcing a generator change silently).
- [ ] 5.4 Review the single-character and positional stress helpers (`singleCharacters`, `validUTF16Offset`, expectation-deriving tests) for alphabet-change fallout; `validUTF16Offset` needs no change (each new operand is a single BMP scalar). Justify every touched expectation in the task notes.

## 6. Verification and Bookkeeping

- [ ] 6.1 Run the full suite (`swift test`) clean. Re-verify both DEF-016 repros by hand against the built library and confirm the observed leaf shapes and empty `leafSeamViolations` match what the regression tests assert.
- [ ] 6.2 Update `DEFECTS.md`: flip DEF-016 to `fixed` with the fix summary (predicate generalized per the root-cause note — CRLF machinery was the special case — unconditional seam checks at the insert post-splice site and the delete merge gate, stress alphabet extended) and the commit reference.
- [ ] 6.3 Add a `CHANGELOG.md` entry under `[Unreleased]` → `Fixed`, user-visible terms: inserting or deleting near chunk boundaries could leave a grapheme cluster (e.g. a combining mark and its base) spanning two internal chunks, violating the rope's structural invariant; seam repair now covers all grapheme clusters, with CRLF as one case. Note: no API or content behavior change — leaf-boundary placement only.
- [ ] 6.4 Run `openspec validate fix-grapheme-seam-repair --strict` from the repo root and resolve any findings.
- [ ] 6.5 Run the final full `swift test` and record suite totals in the task notes.
