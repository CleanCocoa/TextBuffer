## 1. Red: Exit Tests That Fail Against Current `main`

- [x] 1.1 Extend the existing `TextRopeDeleteIntRangePreconditions` suite in `Tests/TextRopeTests/TextRopeDeleteTests.swift` (its `await #expect(processExitsWith: .failure)` pattern) with the DEF-015 delete repros on a 5-unit rope (`TextRope("hello")`): `delete(in: 500..<500)` (empty range past the end) and `delete(in: (-1) ..< (-1))` (empty range at a negative location). Both MUST fail today — the `isEmpty` early return bypasses the preconditions and the process exits normally.
- [x] 1.2 Add a first exit-test suite for insert to `Tests/TextRopeTests/TextRopeInsertTests.swift` (model it on `TextRopeDeleteIntRangePreconditions`; none exists there yet): on `TextRope("hello")`, `insert("", at: 500)` and `insert("", at: -1)` each in an `await #expect(processExitsWith: .failure)` block. Both MUST fail today. Also pin the already-trapping non-empty case `insert("x", at: 500)` in the same suite — it MUST pass today (the insert spec's new trap scenario covers both operand kinds).
- [x] 1.3 Add the `NSRange` wrapper case to `Tests/TextBufferTests/TextRopeNSRangeParityTests.swift` (NSRange tests live in TextBufferTests — TextRopeTests is Foundation-free): `delete(in: NSRange(location: 500, length: 0))` on `TextRope("hello")` MUST exit with failure. MUST fail today. Put it in a separate suite (or clearly separated test) rather than inside `TextRopeNSRangeWrapperPreconditions`, whose doc comment states its traps provably come from the wrapper's own `NSRange` validation — this trap intentionally comes from the forwarded primitive.
- [x] 1.4 Green pins for the preserved no-ops (MUST pass before *and* after the fix): on `TextRope("hello")`, `delete(in: 3..<3)` and `delete(in: 5..<5)` leave `content == "hello"` and `utf16Count == 5`; `insert("", at: 0)`, `insert("", at: 3)`, and `insert("", at: 5)` likewise. Add them next to the existing empty-operand tests in `TextRopeDeleteTests.swift` / `TextRopeInsertTests.swift` if not already covered at these boundary offsets.
- [x] 1.5 Run `swift test` and record the red list: the 1.1 pair, the 1.2 empty-string pair, and 1.3 MUST fail; 1.2's non-empty pin and all of 1.4 MUST pass. Any deviation means a repro is wrong — stop and re-derive before implementing.
  > [2026-08-04] Red list matched the prediction exactly: 5 failures — the 1.1 pair (`deleting traps for an empty range past the end`, `deleting traps for an empty range at a negative location`), the 1.2 empty-string pair (`inserting an empty string traps for an offset past the end`, `inserting an empty string traps for a negative offset`), and 1.3 (`delete traps for a zero-length range at an out-of-bounds location`). 1.2's non-empty pin (`inserting traps for an offset past the end`) and all 1.4 no-op pins passed; XCTest side 631/631 green.

## 2. Green: Preconditions Before Empty-Operand Early Returns

- [x] 2.1 In `Sources/TextRope/TextRope+Delete.swift`, move `if utf16Range.isEmpty { return }` (currently line 6) *after* the two bounds preconditions (currently lines 7-8). The early return stays before `ensureUnique()`, so an in-bounds empty delete performs no path copy and leaves the tree untouched. Verify 1.1 goes green.
- [x] 2.2 In `Sources/TextRope/TextRope+Insert.swift`, move `if string.isEmpty { return }` (currently line 4) *after* the offset precondition (currently line 5). The early return stays before `ensureUnique()`. Verify 1.2 goes green.
- [x] 2.3 Confirm `Sources/TextBuffer/TextRope+NSRange.swift` needs no source edit: the delete wrapper forwards `location ..< location + length` to the primitive, whose bounds checks now run for zero-length ranges — 1.3 going green without wrapper changes is the proof. Check the wrapper's header comment and the `TextRopeNSRangeWrapperPreconditions` doc comment still read accurately now that the primitive traps zero-length out-of-bounds forwards; adjust wording only if inaccurate.
  > [2026-08-04] No source edit; wrapper header comment still accurate. The `TextRopeNSRangeWrapperPreconditions` doc comment's "zero lengths included, so the traps provably come from the wrapper" claim was no longer true (the primitive now traps those forwards too) — reworded to say the wrapper's preconditions merely run first, pointing at `TextRopeNSRangeForwardedPreconditions` for the inherited trap.
- [x] 2.4 Confirm `replace(range:with:)` is unaffected (its own preconditions in `Sources/TextRope/TextRope+Replace.swift` run before composing the primitives) — existing replace precondition exit tests in `Tests/TextRopeTests/TextRopeReplaceTests.swift` stay green, no new tests needed.
- [x] 2.5 Run `swift test` — the full suite MUST be green, including the 1.4 no-op pins and the whole pre-existing suite.

## 3. Verification and Bookkeeping

- [x] 3.1 Update `DEFECTS.md`: flip DEF-015 from `open` to `fixed`, appending the fix note in the file's existing style (fix date, commit, change ID `fix-empty-operand-oob-mutations`), noting the identical DEF-004 decision applied — preconditions before empty-operand early returns, in-bounds empty operations still no-ops.
  > [2026-08-04] Commit hash written as `pending` — the fix note is authored before the commit exists; the committer should replace it with the real hash.
- [x] 3.2 Update `CHANGELOG.md`: add a `## [Unreleased]` section (keepachangelog convention) with a `### Changed` entry disclosing the behavior tightening — `TextRope.delete(in:)` with an empty out-of-bounds range, `insert("", at:)` with an out-of-bounds offset, and the TextBuffer `NSRange` delete wrapper with a zero-length out-of-bounds location now trap instead of silently succeeding; in-bounds empty operands remain no-ops; `RopeBuffer`/`SendableRopeBuffer` unaffected (they validate ranges before delegating).
- [x] 3.3 Run `openspec validate fix-empty-operand-oob-mutations --strict` and the full `swift test` suite one final time before archiving.
