## 1. Red: DEF-017 Regression Tests

- [ ] 1.1 Create `Tests/TextRopeTests/DeleteMergeStarvationTests.swift` with the minimal deterministic repro distilled from seed `0xDEF007` iteration 48, without the stress machinery. Hand-build the three-leaf shape (tests assign `rope.root` directly, as `TextRopeCOWTests` does; `Node.inner`/`Node.leaf` compute summaries and heights): `root = .inner([leaf("a"×1074), leaf("b"×1024), leaf("c"×7 + "\u{1F389}" + "d"×1023)])`. Assert the shape precondition first (leaf UTF-8 sizes `[1074, 1024, 1034]`, `chunkSizeViolations` empty, `verifyTreeInvariants` green) so the repro cannot silently stop reproducing. Then `delete(in: 2097..<2105)` — one UTF-16 unit off the middle leaf's tail plus the seven `c`s, leaving the pair `[1023, 1027]` whose 2050-byte combination is starved by the `\u{1F389}` spanning its `[1024, 1026]` window while the left neighbor's 2097-byte combination has boundaries throughout `[1024, 1073]`. Assert `chunkSizeViolations(in: rope.root)` is empty, `leafSeamViolations` empty, `content`/counts against a `String` oracle, and `verifyTreeInvariants(rope)`. MUST fail today with exactly the DEF-017 class: "Leaf 1 has 1023 UTF-8 bytes … redistribution with leaf 0 (combined 2097 bytes) would conform". Do not pin post-fix byte shapes beyond the predicates — assert via the validator helpers, not magic sizes (the conforming outcome, e.g. `[1049, 1048, 1027]`, MAY be recorded in a comment).
- [ ] 1.2 Add the cross-parent variant to the same file — the shape D1/D3 exist for, where the absorbing neighbor lives under a different parent: `root = .inner([P1, P2])` with `P1 = .inner([leaf("e"×1500), leaf("f"×1500), leaf("g"×1500), leaf("a"×1074)])` and `P2 = .inner([leaf("b"×1024), leaf("c"×7 + "\u{1F389}" + "d"×1023), leaf("h"×1500), leaf("i"×1500)])` (both inner nodes at `minChildren`, so no child-count trigger can mask the gate). Assert shape preconditions, then `delete(in: 6597..<6605)` (same seam recipe: the `b`|`c` adjacency, both leaves inside `P2`, absorber `a`×1074 at `P1`'s right edge). Assert `chunkSizeViolations` empty, content/counts against the oracle, `verifyTreeInvariants`. MUST fail today: `P2`'s within-parent merge emits the 1023 leaf at its own left edge with `merged` empty, `P2` keeps 4 children so `deleteFromInner(root)`'s gate never opens, and the validator flags the 1023 leaf against the cross-parent 1074 neighbor.
- [ ] 1.3 Add the fixed-point non-regression guard: the both-sides-starved trio `root = .inner([leaf("a"×1022 + "\u{1F389}"), leaf("b"×1024), leaf("c"×7 + "\u{1F389}" + "d"×1023)])` (left combination 2049 starved — window `[1024, 1025]` inside the trailing emoji; right combination 2050 starved as in 1.1), same seam delete (`delete(in: 2047..<2055)` — leaf 1's trailing emoji is one `Character` but two UTF-16 units, so the seam sits at 1024 + 1024; assert the offsets via the shape precondition). Assert the starved pair is accepted: `chunkSizeViolations` empty, no oscillation — apply one further unrelated operation (e.g. an append at the document end) and assert the starved pair's leaf sizes are unchanged. SHOULD pass today and MUST keep passing after sections 2–3: the new consultation must not disturb a leaf that is genuinely starved against **each** adjacent leaf (design D4).
- [ ] 1.4 Run `swift test --filter DeleteMergeStarvationTests` and record the red list in this file. 1.1 and 1.2 MUST fail with the DEF-017 chunk-size class (no seam violations); 1.3 SHOULD pass. If a MUST-fail case passes, the repro is wrong: re-derive against DEFECTS.md's DEF-017 entry before implementing.

## 2. Green: Same-Parent Other-Side Consultation

- [ ] 2.1 In `Sources/TextRope/Node+Merge.swift`, implement design D2: when `combinedLeaf`'s `rebalancedChunks` output has an undersized first chunk and `merged` is non-empty, make a single redistribution attempt with `merged.last` — if `Node.isBoundaryStarved((merged.last.chunk + chunks[0])[...])` accept the undersized chunk as today (it is now provably starved against both sides); otherwise replace `merged.last` and the chunk with the `Node.rebalancedChunks` output of the combination (guaranteed conforming two-way by `!isBoundaryStarved`, so the attempt cannot cascade). Exactly one attempt per emission — no rescan of `merged`, no loop. Document at the site: the predicate is shared with the validator's `isStarvationJustified` (producer/validator agreement, as at `redistributeStarvedEdge` and `graphemeSeam`), and the right-edge output needs no counterpart because the existing loop consults it rightward and the end-of-list `popLast` branch leftward.
- [ ] 2.2 Run `swift test --filter DeleteMergeStarvationTests`: 1.1 MUST now be green; 1.2 MUST still be red (the gate at `deleteFromInner` has not changed — record this as evidence that the cross-parent wiring is a real, separate mechanism); 1.3 MUST still be green. Record which cases moved.

## 3. Green: Cross-Parent Edge Wiring

- [ ] 3.1 In `Sources/TextRope/Node+Merge.swift`, add `absorbableStarvedEdge(between left: Node, and right: Node) -> Bool` per design D3: spine-walk to `left`'s rightmost and `right`'s leftmost leaf (mirroring `graphemeSeam(between:)`), return `true` iff at least one edge leaf is below `Node.minChunkUTF8` and the combined edge chunks are not `Node.isBoundaryStarved`. Document the validator pairing and that adjacency is document-order per the rope-core-types clarification (design D1).
- [ ] 3.2 In `Sources/TextRope/TextRope+Delete.swift`, extend the merge gate in `deleteFromInner` (currently `childBecameUndersized || !indicesToRemove.isEmpty || hasGraphemeSeam(node)`) with a fourth or-term scanning adjacent child pairs with `absorbableStarvedEdge` (a `hasAbsorbableStarvedEdge(_:)` helper beside `hasGraphemeSeam(_:)`), so the gate opens for exactly 1.2's shape: nothing removed, no child reported undersize, no seam.
- [ ] 3.3 In `mergeUndersizedInnerNodes` (`Node+Merge.swift`), add the `absorbableStarvedEdge(between: current, and: node.children[i])` disjunct to the combine condition beside `graphemeSeam`, so the opened gate funnels the pair through `combinedInner` — whose recursive `mergeUndersizedChildren` makes the pair same-parent and lets 2.1 repair it. `mergeUndersizedLeaves`' combine condition gets NO new disjunct (the undersize term plus 2.1 already cover leaf arrays — design D3). Verify the termination reasoning at the site: after the combine, the predicate is false for the repaired pair (conforming or two-sided starved), and `combinedInner`'s mid-split cannot recreate it (leaf adjacency is grouping-independent).
- [ ] 3.4 Run `swift test --filter DeleteMergeStarvationTests` — all of section 1 MUST be green. Then run the full `swift test`; diff any other test that changed behavior and justify each touched expectation in this task's notes before amending it (content and count assertions must be untouched by construction — only leaf-boundary placement may change).

## 4. Stress-Alphabet Completion (deferred DEF-016 coverage half)

- [ ] 4.1 In `Tests/TextRopeTests/TextRopeStressTests.swift`, add `"\u{301}"`, `"\u{200D}"`, and `"\u{FE0F}"` to `stressCharset` and delete the dated blocked-note comment (2026-08-04, fix-grapheme-seam-repair task 5.1) that defers to this change; keep a short comment naming DEF-016's rationale (lone extenders join leftward, forming clusters across pre-existing adjacencies — the class a pool of self-contained characters cannot produce).
- [ ] 4.2 Run all five pinned seeds over the extended alphabet: `testPerOperationInvariantValidation` (seed `0xDEF007`, per-operation validation, byte-exact oracle) and `testRandomOperationsMatchString` (seeds 0, 42, 12345, `UInt64.max`). **All five MUST be green — this is the change's headline acceptance criterion** (red on pre-change main with exactly the DEF-017 chunk-size class and zero seam violations, per fix-grapheme-seam-repair's 5.3 evidence). Record the run results. Contingency (design Risks): if any violation appears with an insert-path shape (an insert/replace-insert operation index, `chunkSizeViolations` at a carved-run edge), STOP — do not revert the alphabet and do not paper over: record the evidence, then either extend the same edge consultation at the insert unwind sites within this change (if small and provably terminating by the same argument) or file it as a new DEFECTS.md entry with the repro, mirroring how DEF-017 itself was filed.
- [ ] 4.3 Re-verify the helper fallout findings recorded by fix-grapheme-seam-repair 5.4 still hold with the alphabet actually landed: `validUTF16Offset` unchanged (each extender is one BMP scalar), lone extenders intentionally pass the `singleCharacters` filter, expectation-deriving tests stay green (they derive from the oracle). Justify any touched expectation in the task notes; expected: none.

## 5. Verification and Bookkeeping

- [ ] 5.1 Run the full `swift test` clean. Re-verify the 1.1 and 1.2 repros by hand against the built library (temporary diagnostic, deleted afterward): confirm the pre-fix violating shapes (`[1074, 1023, 1027]` with the flagged 1023 leaf) no longer occur and the post-fix leaf shapes satisfy `chunkSizeViolations == []`, recording the observed shapes in the notes.
- [ ] 5.2 Update `DEFECTS.md`: flip DEF-017 to `fixed` with the fix summary (other-side consultation at the merge's starved-emission site plus the `absorbableStarvedEdge` gate at the deletion path's ancestor levels, adjacency defined document-order in producer/validator agreement; stress alphabet extended with the three extenders and all five pinned seeds green over it) and the commit reference. Also update the header note (line 5) and the DEF-016 entry's deferred-coverage sentence to record that the stress-alphabet half landed here.
- [ ] 5.3 Add a `CHANGELOG.md` entry under `[Unreleased]` → `Fixed`, user-visible terms: deleting near chunk boundaries could leave an internal chunk undersized when its other neighbor could have absorbed it, violating the rope's balance invariant; the delete-path merge now consults both adjacent chunks — including across subtree boundaries — before accepting an undersized result. Note: no API or content behavior change — leaf-boundary placement only.
- [ ] 5.4 Run `openspec validate fix-delete-merge-starvation --strict` from the repo root and resolve any findings. Note for archiving: this change's `rope-stress-testing` delta supersedes the deferral note that fix-grapheme-seam-repair's archived delta left in the canonical spec (the note is removed by this delta's text).
- [ ] 5.5 Run the final full `swift test` and record suite totals in the task notes, including explicit confirmation that all five pinned stress seeds ran green over the 25-entry alphabet.
