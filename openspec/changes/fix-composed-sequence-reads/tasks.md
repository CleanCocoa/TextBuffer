## 1. Prerequisite: Sequencing After `fix-rope-split-point`

- [x] 1.1 Confirm `fix-rope-split-point` is implemented and archived, so ADR-012's grapheme-first chunk bounds are in effect (no chunk seam falls inside a `Character`). The DEF-009 hard `precondition` in 3.7 is only sound under that invariant — do not start implementation before it.

## 2. Red: Regression Tests That Fail Against Current `main`

- [x] 2.1 Create `Tests/TextRopeTests/TextRopeComposedSequencesTests.swift` (the public composed API has no direct tests) with the DEF-002 headline repro: `String(repeating: "\u{1F1E9}\u{1F1EA}", count: 40)`, assert `rope.composedCharacterSequence(at: 130) == "🇩🇪"` and `rope.composedCharacterSequences(in: NSRange(location: 130, length: 1)) == "🇩🇪"`. Must fail with `"🇪🇩"` before any implementation change.
- [x] 2.2 In the same file, sweep every UTF-16 offset of a 100-flag document (`\u{1F1E9}\u{1F1EA}` × 100, 400 units) asserting `composedCharacterSequence(at:)` equals `(text as NSString).substring(with: (text as NSString).rangeOfComposedCharacterSequence(at: offset))`. DEFECTS.md records 136/400 offsets wrong — assert all 400 match.
- [x] 2.3 Add RI window-edge cases to the rope tests: RI run starting at offset 0 (windowStart clamps to 0, must stay correct); RI run at document end; odd-length RI run (lone trailing RI); a single lone RI between non-RI text; RI run preceded and followed by ASCII; offsets straddling the radius boundary (127, 128, 129, 130, 131) relative to the run start; a document whose RI run starts exactly at `location - 128`.
- [x] 2.4 Pin the *unaffected* rules so D3's argument cannot decay silently: combining marks (`e\u{301}` repeated past offset 130), ZWJ emoji chains (👨‍👩‍👧‍👦 repeated past offset 130), and a skin-tone modifier sequence — each swept across offsets and compared against full-document `NSString` expansion. These MUST be green before *and* after the fix (they pin the edge-touch retry, not the new anchor).
- [x] 2.5 Extend `Tests/TextBufferTests/RopeBufferDriftTests.swift` under the existing `MARK: - Composed Character Sequence Reads` (`:179-214`) with `testUnsafeCharacterAtRegionalIndicatorRunMatchesMutableStringBuffer`: sweep every offset of a 100-flag document asserting `RopeBuffer` *and* `SendableRopeBuffer` equal `MutableStringBuffer`, following the existing `testUnsafeCharacterAtSurrogateHalvesAndCombiningMarksMatchesMutableStringBuffer` pattern (`:206-214`).
- [x] 2.6 Add `testContentInRegionalIndicatorRunMatchesMutableStringBuffer` to the same block, driving the existing `assertContentInMatches` helper (`:181-185`, which already checks both rope buffers) over sub-ranges inside, spanning, and ending in an RI run — including ranges that start mid-flag on a trail surrogate.
- [x] 2.7 Add a drift case for RI runs mixed with ASCII and combining marks in one document (flags after 200 units of prose), so the anchor walk is exercised with a non-RI scalar immediately left of the run.
- [x] 2.8 Add the cap-fallback test: a document of 2,049+ consecutive regional indicators (a contiguous run longer than the fixed 4,096-UTF-16-unit backward-walk cap), reads sampled at offsets deep inside the run compared against full-document `NSString` expansion. MUST fail today (mispairing); after 3.5 it exercises the silent full-document fallback as an ordinary test path. Keep it in the regular suite if it runs in reasonable time; otherwise gate it behind the stress-suite pattern.
- [x] 2.9 Add the DEF-004 exit tests to `Tests/TextRopeTests/TextRopeNavigationPreconditionTests.swift`, following its existing `await #expect(processExitsWith: .failure)` pattern: on a 5-unit rope (`"hello"`), `content(in:)` and `composedCharacterSequences(in:)` each called with a zero-length range at a past-end location (`NSRange(location: 6, length: 0)`), a negative location (`NSRange(location: -1, length: 0)`), and `NSRange(location: NSNotFound, length: 0)`. All six MUST fail today — the empty-range early returns bypass the preconditions and return `""` without exiting.
- [x] 2.10 Run `swift test` and record the red list: 2.1, 2.2, 2.3, 2.5, 2.6, 2.7, 2.8, 2.9 MUST fail; 2.4 MUST already pass. Any deviation means a repro is wrong — stop and re-derive before implementing.

## 3. Green: Parity Anchor, Cap Fallback, and Preconditions

- [x] 3.1 Add `isRegionalIndicator(at:)` to `Sources/TextRope/TextRope+ComposedSequences.swift` — resolve the scalar starting at a UTF-16 offset and test membership in `U+1F1E6...U+1F1FF`, following the leaf access pattern of the existing `isTrailSurrogate(at:)` (`:53-59`).
- [x] 3.2 Add `regionalIndicatorRunStart(before:cappedAt:) -> Int?` — walk backward from a UTF-16 offset over contiguous RI scalars (2 units each) to the first non-RI scalar or offset 0, returning the anchor, or `nil` when the walk exceeds the cap. Walk within the leaf `chunk` and re-descend only at leaf boundaries (design D2 risk note); do not call `findLeaf` per code unit.
- [x] 3.3 Define the cap constant as a `private static let` of 4,096 UTF-16 units (2,048 regional indicators) with a doc comment naming DEF-002, stating the silent full-document fallback, and stating that the value is deliberately *not* derived from `Node.maxChunkUTF8` — the cap bounds RI-run walking, a property of the text, not of chunk geometry (resolved 2026-08-01).
- [x] 3.4 Wire the anchor into `expandingWindow` (`:32-51`): after the existing trail-surrogate adjustment of `windowStart`, if `windowStart` is inside an RI run, snap it to the run start. Leave `windowEnd` untouched — GB12/13 context is left-only and the trailing edge is already covered by the edge-touch retry (design D1).
- [x] 3.5 Implement the cap fallback: when `regionalIndicatorRunStart` returns `nil`, abandon windowing for this call and evaluate `expand` over the full document (`content as NSString` with `local = utf16Range`), returning its result directly. The fallback is silent — no `assertionFailure`, no logging — so 2.8 drives it as an ordinary path in every build configuration. Verify 2.1, 2.2, and 2.8 now pass.
- [x] 3.6 DEF-004: move the bounds preconditions *before* the empty-range early returns in `content(in:)` (`Sources/TextRope/TextRope+Navigation.swift:30-33`) and `composedCharacterSequences(in:)` (`Sources/TextRope/TextRope+ComposedSequences.swift:9-12`). Keep the `length == 0 → ""` early return for in-bounds locations *after* the preconditions — the canonical empty-range scenario (`0 <= location <= utf16Count`) still returns `""`. Verify 2.9's exit tests pass.
- [x] 3.7 DEF-009: after materializing the window, `precondition(window.length == windowEnd - windowStart, ...)` with a message naming DEF-009 and ADR-012. Hard trap in all build configurations — no release fallback, no clamping of `local`: under ADR-012 a chunk seam can never fall inside a `Character`, so a mismatch can only mean a broken rope-internal invariant, and no text computed against a shifted window is correct to return. The desync state is structurally unreachable, so no test covers this trap — do not fake tree state to reach it.
- [x] 3.8 Update the `expandingWindow` doc comment to state the invariants: the window start is anchored to an RI-run boundary so GB12/13 pairing matches the document; runs longer than the cap fall back silently to full-document expansion; and the window length is guaranteed by ADR-012 and enforced by the precondition.
- [x] 3.9 Run `swift test` — the full suite (all 2.x tests plus the pre-existing suite) MUST be green. Confirm no test outside the composed-read and precondition areas changed behavior.

## 4. Verification and Bookkeeping

- [ ] 4.1 Confirm `SendableRopeBuffer` needs no source edit: both `RopeBuffer.swift:49-62` and `SendableRopeBuffer.swift:52-64` delegate straight to `rope.composedCharacterSequences(in:)` / `composedCharacterSequence(at:)`. The 2.5/2.6 drift assertions covering both types are the proof — state it in the change notes rather than adding a redundant test.
- [ ] 4.2 Sanity-check the non-RI read path is untouched: verify with a debug counter or a targeted read that a pure-ASCII document performs no additional leaf descents versus pre-change (DEF-011 is out of scope, but this fix must not make it worse).
- [ ] 4.3 Confirm the cap fallback in 2.8 actually takes the full-document path (during development: a temporary counter or breakpoint, removed before finishing) and that nothing traps or asserts on that path in a debug build — the fallback's silence is part of the contract.
- [ ] 4.4 Update `DEFECTS.md`: mark DEF-002, DEF-004, and DEF-009 `fixed`, noting the fixing change ID; for DEF-009, note the structural fix is ADR-012 via `fix-rope-split-point`, with the enforcing `precondition` landed here. Leave DEF-011 open with its existing text.
- [ ] 4.5 Update `CHANGELOG.md` under `## [Unreleased]`: a `### Fixed` entry describing the corrected regional-indicator reads as a user-visible content fix for `RopeBuffer` / `SendableRopeBuffer` (not as an internal rope detail), and a `### Changed` entry for the 0.10.0 behavior tightening — zero-length out-of-range reads on `TextRope.content(in:)` and the composed-sequence APIs now trap instead of returning `""`.
- [ ] 4.6 Run `openspec validate fix-composed-sequence-reads --strict` and the full `swift test` suite one final time before archiving.
