## 1. Red: Regression Tests That Fail Against Current `main`

- [ ] 1.1 Create `Tests/TextRopeTests/TextRopeComposedSequencesTests.swift` (the public composed API has no direct tests) with the DEF-002 headline repro: `String(repeating: "\u{1F1E9}\u{1F1EA}", count: 40)`, assert `rope.composedCharacterSequence(at: 130) == "🇩🇪"` and `rope.composedCharacterSequences(in: NSRange(location: 130, length: 1)) == "🇩🇪"`. Must fail with `"🇪🇩"` before any implementation change.
- [ ] 1.2 In the same file, sweep every UTF-16 offset of a 100-flag document (`\u{1F1E9}\u{1F1EA}` × 100, 400 units) asserting `composedCharacterSequence(at:)` equals `(text as NSString).substring(with: (text as NSString).rangeOfComposedCharacterSequence(at: offset))`. DEFECTS.md records 136/400 offsets wrong — assert all 400 match.
- [ ] 1.3 Add RI window-edge cases to the rope tests: RI run starting at offset 0 (windowStart clamps to 0, must stay correct); RI run at document end; odd-length RI run (lone trailing RI); a single lone RI between non-RI text; RI run preceded and followed by ASCII; offsets straddling the radius boundary (127, 128, 129, 130, 131) relative to the run start; a document whose RI run starts exactly at `location - 128`.
- [ ] 1.4 Pin the *unaffected* rules so D3's argument cannot decay silently: combining marks (`e\u{301}` repeated past offset 130), ZWJ emoji chains (👨‍👩‍👧‍👦 repeated past offset 130), and a skin-tone modifier sequence — each swept across offsets and compared against full-document `NSString` expansion. These MUST be green before *and* after the fix (they pin the edge-touch retry, not the new anchor).
- [ ] 1.5 Extend `Tests/TextBufferTests/RopeBufferDriftTests.swift` under the existing `MARK: - Composed Character Sequence Reads` (`:179-214`) with `testUnsafeCharacterAtRegionalIndicatorRunMatchesMutableStringBuffer`: sweep every offset of a 100-flag document asserting `RopeBuffer` *and* `SendableRopeBuffer` equal `MutableStringBuffer`, following the existing `testUnsafeCharacterAtSurrogateHalvesAndCombiningMarksMatchesMutableStringBuffer` pattern (`:206-214`).
- [ ] 1.6 Add `testContentInRegionalIndicatorRunMatchesMutableStringBuffer` to the same block, driving the existing `assertContentInMatches` helper (`:181-185`, which already checks both rope buffers) over sub-ranges inside, spanning, and ending in an RI run — including ranges that start mid-flag on a trail surrogate.
- [ ] 1.7 Add a drift case for RI runs mixed with ASCII and combining marks in one document (flags after 200 units of prose), so the anchor walk is exercised with a non-RI scalar immediately left of the run.
- [ ] 1.8 Add a DEF-009 window-length test if the degenerate state is reachable: construct a rope whose chunk boundary falls mid-scalar (via the `Node+Split.swift:87-88` fallback) and assert composed reads over that boundary match `MutableStringBuffer`. If the state cannot be constructed without new internal seams, record that in the test file as a documented gap and rely on 2.6's assertion instead — do not fake tree state.
- [ ] 1.9 Run `swift test` and record the red list: 1.1, 1.2, 1.3, 1.5, 1.6, 1.7 (and 1.8 if constructible) MUST fail; 1.4 MUST already pass. Any deviation means the repro is wrong — stop and re-derive before implementing.

## 2. Green: Parity Anchor and Window Invariant

- [ ] 2.1 Add `isRegionalIndicator(at:)` to `Sources/TextRope/TextRope+ComposedSequences.swift` — resolve the scalar starting at a UTF-16 offset and test membership in `U+1F1E6...U+1F1FF`, following the leaf access pattern of the existing `isTrailSurrogate(at:)` (`:53-59`).
- [ ] 2.2 Add `regionalIndicatorRunStart(before:cappedAt:) -> Int?` — walk backward from a UTF-16 offset over contiguous RI scalars (2 units each) to the first non-RI scalar or offset 0, returning the anchor, or `nil` when the walk exceeds the cap. Walk within the leaf `chunk` and re-descend only at leaf boundaries (design D2 risk note); do not call `findLeaf` per code unit.
- [ ] 2.3 Define the cap constant (4096 UTF-16 units / 2048 regional indicators) as a `private static let` with a doc comment naming DEF-002 and the fallback behavior.
- [ ] 2.4 Wire the anchor into `expandingWindow` (`:32-51`): after the existing trail-surrogate adjustment of `windowStart`, if `windowStart` is inside an RI run, snap it to the run start. Leave `windowEnd` untouched — GB12/13 context is left-only and the trailing edge is already covered by the edge-touch retry (design D1).
- [ ] 2.5 Implement the cap fallback: when `regionalIndicatorRunStart` returns `nil`, abandon windowing for this call and evaluate `expand` over the full document (`content as NSString` with `local = utf16Range`), returning its result directly. Verify 1.1/1.2 now pass.
- [ ] 2.6 DEF-009: after materializing the window, compare `window.length` against `windowEnd - windowStart`; on mismatch `assert` with a message naming DEF-009 and `Node+Split.swift:87-88`, and in release take the same full-document path as 2.5. Do not clamp `local` silently (design D4).
- [ ] 2.7 Update the `expandingWindow` doc comment to state both invariants: the window start is anchored to an RI-run boundary so GB12/13 pairing matches the document, and the window length is verified against the requested span.
- [ ] 2.8 Run `swift test` — the full suite (all 1.x tests plus the pre-existing suite) MUST be green. Confirm no test outside the composed-read area changed behavior.

## 3. Verification and Bookkeeping

- [ ] 3.1 Confirm `SendableRopeBuffer` needs no source edit: both `RopeBuffer.swift:49-62` and `SendableRopeBuffer.swift:52-64` delegate straight to `rope.composedCharacterSequences(in:)` / `composedCharacterSequence(at:)`. The 1.5/1.6 drift assertions covering both types are the proof — state it in the change notes rather than adding a redundant test.
- [ ] 3.2 Sanity-check the non-RI read path is untouched: verify with a debug counter or a targeted read that a pure-ASCII document performs no additional leaf descents versus pre-change (DEF-011 is out of scope, but this fix must not make it worse).
- [ ] 3.3 Verify the fallback path is actually reachable and correct: build a document exceeding the cap (2049+ consecutive regional indicators) and assert reads still match `MutableStringBuffer`. Keep this test if it runs in reasonable time; otherwise gate it behind the stress-suite pattern.
- [ ] 3.4 Update `DEFECTS.md`: mark DEF-002 and DEF-009 `fixed`, noting the fixing change ID. Leave DEF-001, DEF-004, and DEF-011 open with their existing text (this change deliberately does not close them).
- [ ] 3.5 Add a `### Fixed` entry under `## [Unreleased]` in `CHANGELOG.md` describing the corrected regional-indicator reads as a user-visible content fix for `RopeBuffer` / `SendableRopeBuffer`, not as an internal rope detail.
- [ ] 3.6 Run `openspec validate fix-composed-sequence-reads --strict` and the full `swift test` suite one final time before archiving.
